apiVersion: v1
kind: Pod
metadata:
  name: crashloop-app
  labels:
    app: crashloop
spec:
  containers:
  - name: app
    image: busybox
    command: ["sh", "-c", "exit 1"]  # Intentional error: immediate exit
    resources:
      requests:
        memory: "32Mi"
        cpu: "100m"
      limits:
        memory: "64Mi"
        cpu: "200m"
